# https://taskfile.dev

version: '3'

tasks:
  default:
    cmds:
      - cmd: |
          cmake -B build -S . || { rm -rf build && cmake -B build -S .; }
      - cmd: |
          cmake --build build
